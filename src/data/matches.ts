import type { Match } from '../types/match'

/**
 * Anciennes listes démo — conservées vides : le calendrier et le hub viennent uniquement de
 * `MatchesContext` (SportMonks quand une clé est configurée).
 */
export const carouselMatches: Match[] = []
export const upcomingMatches: Match[] = []
