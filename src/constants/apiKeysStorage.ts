/** localStorage — jamais de secrets dans le code source. */
export const LS_KEY_SPORTMONKS_TOKEN = 'talkfoot:sportmonks_token'

/** Émis après sauvegarde / suppression des clés (même onglet). `storage` gère les autres onglets. */
export const API_TOKENS_CHANGED_EVENT = 'tf-api-tokens-changed'
