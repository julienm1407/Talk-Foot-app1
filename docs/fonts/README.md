# Polices Talk Foot

Pour activer la police **Bigail** (charte Talk Foot), placez les fichiers de police dans ce dossier :

- `Bigail.woff2` (recommandé)
- `Bigail.woff` (fallback)

En l’absence de ces fichiers, la police **Bebas Neue** (Google Fonts) est utilisée comme repli.
